TODO:

ADICIONALES:

[gui::IDE]: Atajos de teclado
[gui::IDE]: Click derecho en filetreeview y versionview
[listener::WebDav,webservice::Auth,User]:
	opcion de Activar/desactivar webdav
	acceso a webdav por IP
[webservice::WC, gui::IDE]: terminar status()

[gui::IDE]: Editor mejorado:
	ver salto de linea para lineas largas
	ver numero de linea
	ver . en vez de <tab>
	resaltar linea actual
	resaltar sintaxis

[gui::IDE, webservice::Repos]:Visor de diferencias

BUGS:

[gui::IDE]:
	* no desactiva el tab(o avisa) de un archivo modificado en WC(delete, commit, write)
revision general

[gui::IDE]:
	clone()
	recargar status() en intervalos de tiempo
[webservice::WC]:
	wc/ls -"/"
	wc/ls +status()
