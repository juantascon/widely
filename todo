Treeview + FS:
	configurar click derecho
	modificacion de jerarquia de fs (crear, mover, renombrar, borrar, actualizar, commit, ver otra version, ...)

Editor + FS:
	save a fs webservice

Compilador:
	seleccionar lenguaje
	interfaz con el compilador (webservice?)
	resultado de la compilacion

Downloader:
	descargar archivos empaquetados (webservice?)
	samba? / webdav?

Atajos:
	atajos de teclado
	atajos con iconos

Configurador:
	global
	por cada modulo
	load/save config

Usuarios:
	usuario admin
	manejo de cookies/auth (webservice)
	config de usuarios (add/delete)

Permisos:
	permiso para leer
	permiso para escribir
	permisos por archivos y proyectos ?

Editor 2:
	ver salto de linea para lineas largas
	ver numero de linea
	ver . en vez de <tab>
	resaltar linea actual
	resaltar sintaxis

Diff:
	visor de diferencias ?

